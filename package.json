{
  "name": "nginx.autoindex",
  "version": "1.0.0",
  "description": "autoindex for nginx",
  "private": true,
  "author": "Jack.Chan",
  "scripts": {
    "build": "yarn run gulp"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-htmlmin": "^5.0.1",
    "gulp-replace": "^1.0.0"
  }
}
